version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: activities_user
      POSTGRES_PASSWORD: activities_pass
      POSTGRES_DB: activities_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U activities_user -d activities_db"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - coolify

  backend:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    labels:
      - "coolify.managed=true"
      - "coolify.proxy.port=8000"
      - "traefik.enable=true"
      - "traefik.http.routers.backend-${COOLIFY_RESOURCE_UUID}.rule=Host(`api.skillio.live`)"
      - "traefik.http.services.backend-${COOLIFY_RESOURCE_UUID}.loadbalancer.server.port=8000"
    environment:
      DATABASE_URL: postgresql://activities_user:activities_pass@postgres:5432/activities_db
      JWT_SECRET_KEY: your-jwt-secret-key-change-in-production
      JWT_ALGORITHM: HS256
      JWT_EXPIRE_MINUTES: 1440
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - coolify

  frontend:
    build:
      context: .
      dockerfile: ./frontend/Dockerfile
    labels:
      - "coolify.managed=true"
      - "coolify.proxy.port=3000"
      - "traefik.enable=true"
      - "traefik.http.routers.frontend-${COOLIFY_RESOURCE_UUID}.rule=Host(`skillio.live`)"
      - "traefik.http.services.frontend-${COOLIFY_RESOURCE_UUID}.loadbalancer.server.port=3000"
    environment:
      NEXT_PUBLIC_API_URL: https://api.skillio.live
    depends_on:
      backend:
        condition: service_started
    networks:
      - coolify

volumes:
  postgres_data:

networks:
  coolify:
    external: true
